import 'package:flutter/material.dart';

import 'package:get/get.dart';

class Register2View extends GetView {
  const Register2View({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('Register2View'),
          centerTitle: true,
        ),
        body: SafeArea(
            child: Column(
          children: [],
        )));
  }
}
